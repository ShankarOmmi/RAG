YouTube RAG Assistant

A simple Retrieval-Augmented Generation (RAG) application that allows users to ask questions about a YouTube video and receive answers grounded in the video's transcript.

The system extracts the transcript, converts it into embeddings, retrieves the most relevant parts using vector search, and generates an answer using a Large Language Model.

Interface is built using Streamlit for easy interaction.